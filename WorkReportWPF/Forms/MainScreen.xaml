<Page x:Class="WorkReportWPF.MainScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="668" d:DesignWidth="1300"
      Title="Home">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="650"/>
            <ColumnDefinition Width="650"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="85"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="65"/>
        </Grid.RowDefinitions>
        <Label Content="Dasboard for EOT TST Technician Department" Grid.ColumnSpan="2" Foreground="White" Background="Transparent" FontSize="36" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>

        <GroupBox
  Header="Last of TOP 5 reports"
  Style="{DynamicResource MaterialDesignCardGroupBox}"
  Margin="10,10,10,10"
  materialDesign:ColorZoneAssist.Mode="Custom"
  materialDesign:ColorZoneAssist.Background="#aeea00"
  materialDesign:ColorZoneAssist.Foreground="Black" Grid.Row="1" Background="{StaticResource CanvasBrush}">
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <StackPanel
        Orientation="Horizontal" Margin="0,0,407,0">
                        <materialDesign:PackIcon
          Kind="ImageArea"
          Height="48"
          Width="48"
          VerticalAlignment="Center" 
          HorizontalContentAlignment="Center"
          HorizontalAlignment="Center"                  />
                        <TextBlock
          Margin="8,0,0,0"
          VerticalAlignment="Center"
          HorizontalAlignment="Center"
          Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
          Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </GroupBox.HeaderTemplate>

        </GroupBox>

        <GroupBox
  Header="Last of TOP 5 tasks"
  Style="{DynamicResource MaterialDesignCardGroupBox}"
  Margin="10,10,10,10"
  materialDesign:ColorZoneAssist.Mode="Custom"
  materialDesign:ColorZoneAssist.Background="#aeea00"
  materialDesign:ColorZoneAssist.Foreground="Black" Grid.Row="1" Background="{StaticResource CanvasBrush}" Grid.Column="1">
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <StackPanel
        Orientation="Horizontal" Margin="0,0,407,0">
                        <materialDesign:PackIcon
          Kind="ImageArea"
          Height="48"
          Width="48"
          VerticalAlignment="Center" 
          HorizontalContentAlignment="Center"
          HorizontalAlignment="Center"                  />
                        <TextBlock
          Margin="8,0,0,0"
          VerticalAlignment="Center"
          HorizontalAlignment="Center"
          Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
          Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </GroupBox.HeaderTemplate>

        </GroupBox>
        <TextBlock TextWrapping="Wrap" Text="" Grid.ColumnSpan="2" Grid.Row="2" Padding="0,0,0,0" Margin="320,5,160,6">
            <StackPanel Width="170" VerticalAlignment="Center" HorizontalAlignment="Left" Height="54" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Clock" Foreground="White" Height="46" Width="58" RenderTransformOrigin="0.5,0.603" VerticalAlignment="Center" ToolTip="Home" Margin="10,0,0,0"/>
                <TextBlock x:Name="Time_Text"  Text="Time" Width="92" Foreground="White" Height="20" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>
            
            <StackPanel Width="180" VerticalAlignment="Center" HorizontalAlignment="Left" Height="54" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ClipboardTextClockOutline" Foreground="White" Height="46" Width="58" RenderTransformOrigin="0.5,0.603" VerticalAlignment="Center" ToolTip="Home" Margin="10,0,0,0"/>
                <TextBlock x:Name="Date_Text"  Text="Date" Width="104" Foreground="White" Height="20" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>
            
            <StackPanel Width="175" VerticalAlignment="Center" HorizontalAlignment="Left" Height="54" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ViewDay" Foreground="White" Height="46" Width="58" RenderTransformOrigin="0.5,0.603" VerticalAlignment="Center" ToolTip="Home" Margin="10,0,0,0"/>
                <TextBlock x:Name="Day_Text"  Text="Day" Width="95" Foreground="White" Height="23" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>
            
            <StackPanel Width="257" VerticalAlignment="Center" HorizontalAlignment="Left" Height="54" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="CalendarWeek" Foreground="White" Height="46" Width="58" RenderTransformOrigin="0.5,0.603" VerticalAlignment="Center" ToolTip="Home" Margin="10,0,0,0"/>
                <TextBlock x:Name="Week_Text"  Text="Number of week" Width="180" Foreground="White" Height="20" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>   
        
        </TextBlock>

    </Grid>
</Page>
